# Example .secrets file — do NOT commit real secrets.
# Copy this to `.secrets` and fill in your real values.
# TOKEN should be the bot token without quotes.
TOKEN=your_discord_token_here

# You can add other secret keys as KEY=value pairs.